# ***************************************************************
# ESPHome secrets — NEVER COMMIT THIS FILE
#
# Copy this to `secrets.yaml` and fill in real values.
# Example (for the ESPHome HA add-on, add via the add-on's
# "Files" tab or edit /config/esphome/secrets.yaml):
#   - name: wifi_ssid
#     value: "MyNetwork"
#
# Or inline-style placeholder (used by the device configs):
#   wifi_ssid: "MyNetwork"
#   wifi_password: "hunter2"
#   api_encryption_key: "base64-encoded-32-byte-key"
#   ota_password: "my-ota-password"
#   fallback_ap_password: "12345678"
# ***************************************************************

wifi_ssid: "REPLACE_ME"
wifi_password: "REPLACE_ME"
api_encryption_key: "REPLACE_ME"
ota_password: "REPLACE_ME"
fallback_ap_password: "REPLACE_ME"

# Per-device keys (used by each node's yaml) go here too, e.g.:
# atom_echo_api_key: "REPLACE_ME"
